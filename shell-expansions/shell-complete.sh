#!/bin/bash
ls $HOME > $(date +%H_%M).txt
: ' 
Here $HOME and the $(Date) are words since $ is a special character not a meta charater
Here Tokenisation took place.

This is a simple statement whic ended with control operator \n(invisible)

>" is a redirection operator. 